# STRAT-012 — Candlestick + S/R confluence overlay

**Status:** `HYPOTHESIS` / `UNVALIDATED` / `DRAFT`  
**Origin:** **`PROJECT_MIX`** as a 001/003 gate (`njqeZc_tYy8` + `wDZXqzdGBDc` tool). Stock charts throughout.  
**Bind:** [`TRANSCRIPT_STRATEGY_BIND.md`](../../../01_research/docs/handoffs/TRANSCRIPT_STRATEGY_BIND.md)  
**Not claimed profitable.**

Video: **five** powerful candlestick patterns; patterns are **not** standalone (need trend, 20 SMA, S/R, confirmation candle). Optional RSI < 30 for hammer.

| # | Pattern | EN bind | Rule table (entry/stop) |
|---|---------|---------|-------------------------|
| 1 | **Hammer** | CONFIRMED (wick **2–3×** body; RSI already oversold / below 30) | Extracted this pass |
| 2 | **Bullish engulfing** | **Named in EN** (~17:26+) | **WAITING** on **detail**, not existence |
| 3 | **Morning star** | **Named in EN** (~28:49+) | **WAITING** on **detail**, not existence |
| 4 | **Dark cloud cover** | **Named in EN** (~32:46+) | **WAITING** on **detail**, not existence |
| 5 | **Inside bar** | **Named in EN** (~36:02+) | **WAITING** on **detail**, not existence |

Do **not** write “four patterns not in the video.” EN names them. WAITING = rule-detail not re-extracted this pass.

**HYPOTHESIS (`PROJECT_MIX`):** allow 001/003 entry only if last closed 5m/15m pattern agrees (hammer at support for CE, etc.). **No spoken** “use hammer to filter Gokul 3m VWAP+ST” or “HAUS MACD.” Auto-detect indicator is **Dhan tool**, definition may differ from textbook — if used, freeze Dhan rules from charts (Tier 2), not blogs.

## Invalidation

Pattern filter no lift; or Dhan auto-detect ≠ our parser (`SOURCE_UNCERTAIN`); or treating WAITING-detail as `NOT_IN_EN`.

## Risk

Lagging “leading” narrative is **unsupported** as proof (math: patterns are still price). Overlay on index options is **`PROJECT_MIX`**.
