# Exam playbook — formulas the candidate would take into industry (our industry)

**HYPOTHESIS** until 06 OOS+NORMAL. **NO_PROMOTE.**

## Product rule (pass the oral)

1. **Lead** on index / MIX lean (Tulchinsky expression).  
2. **Confirm** on CE or PE path (smile + EDA).  
3. **Skip** on FOLLOW-GAP, stale, holiday, NEWS_DAY (Gliner + Derman Badly + Bouchaud).  
4. **Validate** with purged time splits (AFML).  
5. **Publish one ticket** (Kakushadze inventory stays in the drawer).

## Named math we may keep coding

| ID | Formula | Books in the room |
|----|---------|-------------------|
| `MIX-FORM-BETA-RESID` | ε = r_opt − k r_idx | Smile (local k), Derman Badly |
| `MIX-FORM-DIVERGE-Z` | z(ε) window 30 | AFML feature hygiene |
| `MIX-FORM-FOLLOW-GAP` | idx↓ & r_PE≤0 or idx↑ & r_CE≤0 | Dual-tape dealer |
| `MIX-FORM-STRADDLE-RET` | r_CE+r_PE | Crude vol proxy, **not** IV |
| ML-001 overlay | KMeans + IsolationForest | AFML unsupervised first |

k is **OLS on a named book**, not a greek. Re-estimate per expiry; sid dies weekly.

## Token budget (TOKEN_ML)

PhD agent **reads this KB via FTS** (`kind=phd_book_kb`), including `book_kb/topics/*` (theta, gamma, delta, vega/IV, intrinsic, HQ greeks, POC, OI language, levels, MIX-FORM residual, overfit, model pick, RETUNE_GATE). Do not paste chapters into Gemini. Pre/post counsel may cite a **doc_id**, not a page scan.

## Ready for next session

Dual-tape poll + `desk_ml score` + FOLLOW-GAP HOLD. Paper `MIX-DEFAULT-BUY` only. Rebuild RAG after note edits: `python -m agent_rag rebuild`.
