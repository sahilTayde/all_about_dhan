# packages/desk-intel/tests — cheap smoke only

Nightly JSON schema keys: `desk_intel.nightly.RECON_JSON_KEYS` (includes `session_kind`, `retune_proposal`). Full pytest suite is a **TODO** for team 08 (fixtures + automation), not this package. Retune gate smoke: `test_retune_gate.py`.
