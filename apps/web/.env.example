# Optional. Leave empty to load public/mock/signal.json (no network, no Dhan).
# Later, point at the FastAPI app, e.g. http://127.0.0.1:8000
# Expected later: GET ${VITE_API_URL}/paper/signal
VITE_API_URL=
