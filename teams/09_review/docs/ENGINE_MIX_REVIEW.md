# ENGINE_MIX_REVIEW — five-pass NOTES (not a pass)

**Team:** 09_review  
**Date:** 2026-09-03  
**Subject:** [`ENGINE_MIX.md`](../../04_quant/docs/ENGINE_MIX.md) (Phase-1 BULL / BEAR / SIDEWAYS mix of STRAT-001–014)  
**Status:** `NOTES_ONLY`  
**Gate:** `RESEARCH_READY_FOR_PROGRAMMING` = **not issued**  
**Checklist:** [`docs/REVIEW.md`](../../../docs/REVIEW.md)

Education ≠ endorsement. **No win rates.** Docs Auditor PASS on this file is **not** a product gate.

Spot-check of origin claims: [`OPTIONS_INDEX_PACKET.md`](../../01_research/docs/handoffs/OPTIONS_INDEX_PACKET.md) (2RnBT9 / HAUS / gA5 / pvmvki / 6el9 / YUXJv) + TA skip list (`H_6keeRUCDM`, `njqeZc_tYy8`).

---

## English transcript bind (01 → 04, 2026-09-03)

**Honesty:** the prior mix pass **copied STRAT timestamps**. [`TRANSCRIPT_STRATEGY_BIND.md`](../../01_research/docs/handoffs/TRANSCRIPT_STRATEGY_BIND.md) **re-read** English `normalized_en/` (`youtube_translate`). English wins if a STRAT / ENGINE_MIX / Hindi packet disagrees. That improves source bind; it is **not** a five-pass and it does **not** waive red-team **yes**.

**04 cite:** [`ENGINE_MIX.md`](../../04_quant/docs/ENGINE_MIX.md) now points at the bind (header + **Transcript bind** section) and keeps **`PROJECT_MIX` labeled** — not `DHAN-DERIVED`. Clubs 01 named as untaught as one recipe stay tagged: 003+007+009 **AND**, 5m ST/MACD confirm-or-kill, named “Dhan book,” 001 index-only universe, **011**, **012**, 010-as-003-gate. Same-video Gokul stack 003+004+005+008+009 is correctly **not** called PROJECT_MIX. Honesty banner still `research_ready_for_programming: false`. Guests recorded as **affiliation only** (Gokul SEBI RA; Himanshu “Star Trader”; Mukul no RA claim).

**Q4 still FAIL.** 011/012 remain on the mix (`primary_alt` / `overlay_optional`). They are **`PROJECT_MIX`** (H_6kee stocks/gold; njqeZc stock candles; gA5 **executed sell**). Treating them as **Dhan NIFTY law** is a fail. The PROJECT_MIX label is required honesty; it does **not** waive Q4.

**Q19 still FAIL.** Spoken **SEBI registered Research Analyst** / **Star Trader of this Dhan Platform** ≠ validated edge, ≠ license to code, ≠ live blueprint. 04’s guest table says so; the mix is still a founder playbook that maps education onto customer WATCH→CONFIRMED. Affiliation copy does not waive Q19.

**Q8, Q12, Q20 still FAIL.** Bind does not create HQ option history, a fill/reject model, or a unique compute path (3m resample, ST “103” WEAK, 007∩009 AND PROJECT_MIX).

**Do not** five-pass. **Do not** waive. **Do not** set `RESEARCH_READY_FOR_PROGRAMMING`. Verdict below is unchanged: **`NOTES_ONLY`**.

---

## 1. Verdict

```text
verdict:                NOTES_ONLY
five_pass:              NOT PASSED
research_ready_for_programming: false
layer:                  HYPOTHESIS under review (04) — not SOURCE_FACT, not VALIDATION
ids:                    STRAT-001 … STRAT-014 only. No STRAT-015+. Mix IDs MIX-BULL / MIX-BEAR / MIX-SIDEWAYS are compositions, not new STRATs.
metrics:                still null — 09 did not invent any
```

**This is not a five-pass pass.** 04’s honesty banner is accepted. The mix is a named club of existing IDs with origin tags left standing — that is **allowed as a hypothesis**, not as a license to code live signals or to stamp `RESEARCH_READY_FOR_PROGRAMMING`.

Any red-team **yes** below stays **FAILED** for that item. Do not waive because the banner says `UNVALIDATED`.

---

## 2. Pass 1–5 one-liners

| Pass | One-liner |
|------|-----------|
| **1 Source** | 01 bind re-read EN (prior mix copied STRAT ts). 04 cites bind; **`PROJECT_MIX` kept labeled**. 011/012 still attached as PROJECT_MIX, not Dhan NIFTY law. |
| **2 Technical** | ST/VWAP/VWMA/MACD as OHLC compute (no invented series REST) is honest; 12/26/9 on the mix YAML is a **VALIDATION convention**, not HAUS-spoken; 004 lengths still `UNKNOWN`; ST ATR seed vs Dhan charts unsigned. |
| **3 Market** | Futures tape + `FROM_CONTRACT` lots/expiry + SENSEX=BSE are aligned with 03; 3m/2m still not HQ-native; lot + F&O **15:40** circulars **not in-repo**; HQ option history `DATA_INSUFFICIENT`. |
| **4 Quant** | No engine, no OOS, no costs-on-tape, no fill study; default ticket is a large **AND** of two-video clocks + staging + 005 + PERSONA + news; 06 cannot pass this pass on fixtures alone. |
| **5 Red-team** | **FAILED items:** Q4, Q8, Q12, Q19, Q20 (see §3). Notes ≠ pass. |

---

## 3. Red-team Q1–Q20

If the answer is **yes** (failure): that item stays **FAILED**. Unknown is not a waiver of a yes.

| Q | Failure question | Answer | Evidence (one line) |
|---|------------------|--------|---------------------|
| 1 | Skip or invent a transcript? | **no** | Mix timestamps for 003/005/007/008/009/013/014 match OPTIONS_INDEX / HAUS-EN (e.g. 2RnBT9 21:59 3m, 25:58–26:07 call, 38:06–39:20 strike, HAUS-EN-16 55:18–55:42 clocks). |
| 2 | Invent any Dhan recommendation? | **no** | Default ticket is tagged mixed `DHAN-DERIVED` + `PROJECT-DERIVED`; staging confirm-or-kill is desk spec, not a fabricated speaker line. *(Composition is still a project recipe — see Q19.)* |
| 3 | Misinterpret a number? | **no** | Spoken 70/30, POP 80%, lot 65, pvm “555 to 6” not frozen as metrics; ST “103” stays `[UNCERTAIN]` while 10,3 is working default pending chart VERIFY. |
| 4 | Use a stock-only concept for options? | **yes** | **FAILED.** 011/012 still attach (`primary_alt` / `overlay_optional`). Bind + 04 label them **`PROJECT_MIX`** (not Dhan NIFTY law). Tag does not waive Q4. |
| 5 | Confuse underlying data with option data? | **no** | Default YAML `ohlc: FUTURES`, execution OPTIDX BUY, 005 on chain; Greek delta ≠ volume-delta kept. *(006-alt spot-vs-futures still a 03 conflict — §4.)* |
| 6 | Use future information? | **UNKNOWN** | Spec says `look_ahead: forbidden` and parks post-hoc 008 “dominant”; 3m/2m resample method is `UNKNOWN` — unclosed-bar resample can leak until 02 signs a construction. |
| 7 | Use current lot size historically? | **no** | `lot_size: FROM_INSTRUMENT_MASTER`; 2RnBT9 “65 × premium” not frozen (OPTIONS_INDEX + 03). |
| 8 | Use unavailable historical option data? | **yes** | **FAILED.** Default ticket needs chain greeks (005) and 3m OI Δ (L3 EARLY); 03: HQ historical option quotes **DATA_INSUFFICIENT**. Fixtures-later does not waive. |
| 9 | Optimize against the test set? | **no** | No backtest; 003 default is the 2RnBT9 video primary, not an IS return rank. |
| 10 | Ignore transaction costs? | **no** | YAML `costs.include: [brokerage, statutory, half_spread]`; invalidation includes expectancy ≤ 0 after costs. |
| 11 | Ignore bid/ask? | **no** | `half_spread` + `fill: next_bar_open_or_conservative_ask` named (still an assumed fill — Q12). |
| 12 | Assume fills? | **yes** | **FAILED.** Conservative ask / next-bar-open still assumes the option is filled; no reject / gap / RM-lot model (03: 1 ITM lot may exceed RM). |
| 13 | Confuse correlation with causation? | **no** | Profitability not claimed; 70/30 kept as SOURCE_FACT anecdote, not a theorem. |
| 14 | Overfit indicators? | **UNKNOWN** | No IS run. Default ticket ANDs 007∩009 + 008 + 5m ST/MACD + 005 + PERSONA + news — fragile conjunction, not yet a fitted stack. |
| 15 | Pick the winner by highest backtest return? | **no** | No returns exist; 001/006/011 are alt books, not IS champions. |
| 16 | Ignore losing regimes? | **no** | MIX-SIDEWAYS is no-trade; 013/014 WAITING sell; 003 ST≠VWAP skip kept. |
| 17 | Ignore liquidity? | **UNKNOWN** | half_spread named; no hard width/OI/50-multiple gate; SENSEX strike step not extracted (03). |
| 18 | Use another YouTube channel? | **no** | Cited IDs are enabled `@DhanHQ` catalog; no `EXTERNAL_RESEARCH` channel. |
| 19 | Treat education as expert endorsement of a live strategy? | **yes** | **FAILED.** Spoken SEBI RA (Gokul) / Star Trader (Himanshu) ≠ edge. Guest table is affiliation only; mix is still the live-strategy blueprint. |
| 20 | Can another researcher **not** reproduce the result? | **yes** | **FAILED.** There is no result. 3m resample `UNKNOWN`, ST ATR seed `UNKNOWN`, 007∩009 is project-AND not spoken, staging MACD 12/26/9 is convention, 004/010 parked. |

**FAILED (yes):** Q4, Q8, Q12, Q19, Q20.

That set alone blocks a five-pass and blocks `RESEARCH_READY_FOR_PROGRAMMING`.

---

## 4. Cross-talk — disagreements 00 must not paper over

Do **not** merge these into one “Dhan mix.” A pass that collapses any row is a fail.

### 02 vs 04

| Conflict | 02 VALIDATION | 04 ENGINE_MIX | 09 |
|----------|---------------|---------------|-----|
| **007 vs 009 clocks** | **Do not merge.** Grid 14:30 / 14:45 / 15:00 on 007; 009 is a different speaker. | Phase-1 **AND**: no new 09:15–**10:00**, no new after **14:30**, flatten **15:15**. | The AND is a **PROJECT-DERIVED clock**, not DHAN-DERIVED agreement. Keep two filter IDs + a named intersection hypothesis. Do not relabel as “what Dhan said.” |
| **003 bar** | Grid 3m resample **or reject 003 until native 5m**; 5m ST confirm-or-kill is **not** this video’s 3m entry. | YAML `timeframe.signal: 5m` **and** `resample_hypothesis: 3m`; confirm path = HQ 5m. | Two constructions, not one series. 00 must not ship “003 on 5m” as if it were 2RnBT9. |
| **MACD role** | HAUS child-TF MACD **entry** ≠ desk 5m confirm-or-kill. Do not freeze ×3/×4 or 12/26/9 as HAUS. | Customer path = staging; 001 buy-the-5m-high is a **separate** test ID; mix YAML freezes staging MACD **12/26/9**. | Split is accepted. 12/26/9 on MIX-BULL is **convention**, not a Dhan MACD. Do not later “simplify” 001 into the default ticket. |
| **003 as default while resample is UNKNOWN** | Will not own compute path until resample (e.g. 1m→3m OHLC) is proposed or 003 is 5m-only. | 003 remains **default primary** with resample `UNKNOWN`. | Default primary on an unsigned bar is a freeze 02 rejected. Park 003-native-5m vs 003-resample-3m as **two mix_ids**, or 003 is not default. |

### 03 vs 04

| Conflict | 03 VALIDATION | 04 ENGINE_MIX | 09 |
|----------|---------------|---------------|-----|
| **007 ≠ 009** | Reject silent merge. Flatten **15:15** = speaker heuristic, **not** F&O close (**15:40 VERIFY**). Parameterize `close_model`. | AND intersection; YAML has no `close_model`; 009 flatten is the Phase-1 filter. | Spoken 15:15 flatten ≠ exchange close. Do not stamp F&O EXPIRED at 15:15 unless 009 is an **attached hypothesis**, not “the session.” |
| **Strike 002 / 005 / 006** | Three market objects (OTM vs high-delta vs 100–200 pts). 100–200 pts is **not** a SENSEX law. | Default 003 → **005 only**; 002 on 001-alt; 006 on 006-alt. | Alignment on **not merging**. Keep three ablations. Do not average deltas. |
| **006 analysis object** | Speaker **spot OHLC**; futures swap is TEST — do not silently swap. 2m vs desk **3m** chain cannot lockstep. | 006 is alt; MIX-BULL YAML `ohlc: FUTURES` (003 book). | BULL_ALT_006 needs its **own** YAML. Do not run 006 under MIX-BULL’s futures object. |
| **014 Monday 09:45 vs 009 skip** | 09:45 sits **inside** 009’s 09:15–09:45 skip. Do not silently merge. | 014 WAITING — not on the buy ticket. | Standing sell-book conflict. Do not “fix” by moving 014 entry to 10:00. |
| **Chain vs 003 math** | 003 confirm is **FUTIDX** ST/VWAP — 3m chain **not required for that math**. Chain required when **005** picks a strike. | L3 chain Δ is a **leading** EARLY ingredient on the default ticket. | Global desk overlay ≠ 003 SOURCE_FACT. L3 is PROJECT-DERIVED. Do not write it back onto 2RnBT9. |

### 03 vs 05

| Conflict | 03 | 05 SIGNAL_FUSION | 09 |
|----------|----|------------------|-----|
| **When chain gates CONFIRMED** | Required for strike-pick IDs and after MACRO_EVENT; **not** for 001/003/007/008/009 math. | Chain Δ vs last 3m snapshot is a **leading** EARLY ingredient; 05 never promotes CONFIRMED. | Keep both: 03’s per-ID math vs 05’s mix overlay. Do not make PCR/OI the 003 primary. |
| **EXPIRY tag** | 03 / nightly owns `EXPIRY` (sheet date = session date). | `NEWS_DAY` / `EXPIRY` **block EARLY**. | 04 mix EARLY table does **not** name `EXPIRY` (only MACRO_EVENT). See 04 vs 05. |

### 04 vs 05

| Conflict | 04 ENGINE_MIX | 05 SIGNAL_FUSION | 09 |
|----------|---------------|------------------|-----|
| **Mix freeze** | ENGINE_MIX is a frozen **HYPOTHESIS** mix (still UNVALIDATED). | 05 HANDOFF still says **“no mix freeze.”** | 05 is **stale**, not a veto of the file. Update 05 HANDOFF; do not pretend 04 never froze. |
| **Primaries in play** | One primary per ticket; default **003**; 001/006/011 **alt, not same ticket**. | “Overlaying **001, 003, 006**” as trend/scalp primaries. | 05 must not veto/bias as if three primaries can fire together. Bind overlay to the **declared book**. |
| **CONFIRMED rule** | 5m ST **and/or** 5m MACD, **and the other does not contradict**. | Copies [`SIGNAL_STAGING.md`](../../04_quant/docs/SIGNAL_STAGING.md): ST **and/or** MACD **in the same direction** — no explicit contradict clause. | Two promotion rules. 00 must not average them. Staging file vs mix file must be one sentence. |
| **EXPIRY vs EARLY** | EARLY forbidden: MACRO_EVENT undigested, clocks, 008 mixed. **EXPIRY tag not in the EARLY table.** | On `EXPIRY`, **do not** print EARLY. | Refuse to merge. Either expiry-afternoon is a mix-level EARLY block (05) or it is only PERSONA size/EXPIRED (04). Write it in ENGINE_MIX. |

### 02 vs 03 (keep, not new)

Both refuse cash-index VWAP, both refuse 3m as HQ `interval`, both refuse one Dhan strike, both keep 011/012 PROJECT-DERIVED and 013/014 WAITING. **Do not use that agreement to paper over the clock AND or the 003 bar.**

---

## 5. What would still be required before a real pass

A later five-pass still has to run [`docs/REVIEW.md`](../../../docs/REVIEW.md) **after** these exist. Notes now ≠ that pass.

| Need | Why it blocks a pass |
|------|----------------------|
| **Backtest engine (06)** | Pass 4 is empty. `BACKTEST_REQUIRED` nightly JSON is not a backtest. |
| **OOS + walk-forward + two-regime invalidation** | Trend **and** chop; expectancy after costs; no IS winner-picking (Q9/Q15). |
| **Signed 3m/2m resample (02)** or **003/006 native 5m/1m only** | Q6/Q20. Default primary cannot sit on `UNKNOWN` bars. |
| **Chart VERIFY Supertrend ATR seed / HL2 vs close vs Dhan** | 10,3 is convention until mismatch is `SOURCE_UNCERTAIN`, no silent library swap. |
| **Official lot circular + F&O 15:40 circular in-repo (03)** | Q7 stays honest only with as-of lots; Q3/close_model currently VERIFY. |
| **HQ historical option quotes or an honest fixtures-only charter** | Q8 FAILED today. Fixtures cannot be used to claim a pass. |
| **Live `DHAN_*` chain (user ask)** | 3m poll + greeks vs last snapshot unvalidated; empty token → fixtures. |
| **SENSEX BSE strike step / tick / own chain client** | 03 UNKNOWN; 006 100–200 pts is NIFTY-scale folklore. |
| **004 lengths from charts or stay parked** | Do not invent 9/21. |
| **010 OF history or stay parked** | `order_flow: false` stays. |
| **Ablations actually run** | 002 vs 005 vs 006; H-001-HAUS-ENTRY vs H-STAGING-CONFIRM; 3m resample vs 5m native; 007 vs 009 vs AND. |
| **Q4 resolved or 011/012 dropped from Phase-1 attach** | Transfer stays FAILED while those IDs attach to the index buy mix. |
| **Fill model beyond assumed ask** | Q12 FAILED until rejects / RM / gaps exist in 06. |
| **Five-pass after the above, by 09, with zero remaining yes** | This file is **not** that pass. |

---

## 6. Comments back

### TO 02_phd_math

1. Sign **or reject** TV-style ST(10,3) on **FUTIDX** as compute convention. Mismatch vs Dhan charts → `partially_supported`, no silent swap. 09 will not pass 003 on an unsigned ATR seed.
2. **Reject 7,3 on this book** — agreed. Keep `_byuht` on EQ.
3. Staging MACD **12/26/9** on MIX-BULL is a **convention freeze 04 already made**. Either own it as VALIDATION-default for confirm-or-kill, or reject the YAML and force `UNKNOWN` until a grid exists. Do not let 06 treat 12/26/9 as HAUS.
4. Futures VWAP+VWMA(20) = `supported` math **on FUTIDX volume**. Cash-index VWAP stays `unsupported`. Quote `average_price` ≠ session VWAP until proven.
5. **Do not polite-sign 3m.** Propose 1m→3m OHLC (or refuse). 04 left 003 as default with resample `UNKNOWN` — that is the row 09 will fail on a real pass.
6. 004 stays parked. 09 will fail any mix that invents Super Scalper lengths.
7. Keep Greek delta (005) ≠ volume-delta (010). 010 remains `DATA_INSUFFICIENT`.

**Reject this mix as a compute package** until (5) is signed or 003 is native-5m-only.

### TO 03_phd_market

1. Futures tape + SENSEX BSE + no cash-index volume: mix did **not** reintroduce cash VWAP. Sign that slice.
2. **Do not sign 15:15 as F&O close.** Mix uses spoken flatten as a **filter**. Demand `close_model` on the YAML (`spoken_1515` vs `fo_1540 VERIFY`).
3. **Do not sign the 007∩009 AND as market law.** It is a conservative project intersection. Video-specific books remain the VALIDATION position.
4. Strike 002/005/006: mix kept them exclusive. Add the **market** constraints (NIFTY 50-multiple liquidity, BANKNIFTY monthly-only VERIFY, SENSEX step UNKNOWN) onto the ablation sheet — not into a merged strike.
5. `expiry: FROM_CONTRACT` — mix did not freeze Thursday/Tuesday. Hold that.
6. Desk poll **3m** as default: sign. 1m full-chain stays wrong. L3-as-EARLY is **05/04 overlay**, not 003 math — say so on the 003 row so 06 does not require historical OI to test VWAP+ST.
7. Historical option quotes: repeat **DATA_INSUFFICIENT**. 09 already FAILED Q8. Live chain does not rewrite history.

### TO 04_quant

1. Honesty banner: **accepted**. No `RESEARCH_READY_FOR_PROGRAMMING` from 09.
2. §4 conflicts left standing: **accepted** (strike, MACD role, 3m vs HQ, 7,3, 013/014 WAITING). A later “cleanup” that merges 002+005 or HAUS-entry+staging-confirm is an automatic fail.
3. **Fix the clock story.** Either (a) two mix_ids `FILTER_007` vs `FILTER_009` vs `FILTER_AND_PROJECT`, or (b) drop the AND from the default ticket. Do not keep AND in YAML `role.filter: [007, 008, 009]` while telling 02 you did not merge.
4. **Split 003.** `PHASE1_BULL` cannot list `signal: 5m` and `resample_hypothesis: 3m` as one object. Two mix_ids (or 003 parked until 02 signs).
5. MIX-BULL `origin_videos` includes HAUS 001 timestamps though 001 is not on the default ticket — 06 will attach 001 by accident. Default YAML should cite **003/005/007/008/009 only**.
6. BEAR 005 `qa_flag: UNCERTAIN_TRANSCRIPT` vs BULL 005 `null` on the same 38:06–39:20 span — pick one.
7. Add `EXPIRY` to the EARLY forbidden list **or** explicitly reject 05’s block. Do not leave it to PERSONA prose.
8. Align CONFIRMED one-liner with [`SIGNAL_STAGING.md`](../../04_quant/docs/SIGNAL_STAGING.md): either add “other must not contradict” to staging, or drop it from the mix.
9. **011 / 012:** keep `PROJECT-DERIVED`. Q4 is FAILED while they attach. If Phase-1 default is 003-only, **drop them from `primary_alt` / `overlay_optional` until a transfer charter exists** — or accept Q4 forever.
10. BULL_ALT_006 gets its own YAML (`ohlc` spot vs futures TEST). Do not inherit MIX-BULL `FUTURES`.
11. No win rates, no fills invented — **held**. Spoken 70/30 stays anecdote.

### TO 05_analysis

1. News / MACRO_EVENT = veto / no-trade: **accepted**. Extreme PCR-only = veto: **accepted**. 05 must not promote CONFIRMED: **accepted**.
2. Update SIGNAL_FUSION HANDOFF: “no mix freeze” is **false** as of ENGINE_MIX. The freeze is still `UNVALIDATED`.
3. Bind vetoes to the **declared book** (default 003). Stop listing 001+003+006 as if they were concurrent primaries.
4. L3 chain-as-leading is PROJECT-DERIVED desk, not 003 SOURCE_FACT. Keep PCR numeric law **out**.
5. PERSONA_DESK checklist for CONFIRMED: accepted as overlay. Do not invent a second opening-drive T besides 009’s 09:15–09:45 (search param must be named).
6. 010 stays parked. Mock sentiment windows stay mock. CAS book stays WATCH-only.
7. Resolve EXPIRY-blocks-EARLY with 04 in **one** sentence in ENGINE_MIX. 09 will fail a pass that has both rules.

---

## HANDOFF (09 — ENGINE_MIX notes)

**Accepted**

- 14 IDs only; mix_ids are compositions not STRAT-015+.
- Honesty banner; metrics null; 013/014 WAITING; 004/010 parked; 002 vs 005 vs 006 not merged; 011/012 tagged PROJECT-DERIVED; futures OHLC; chain 3m; news as veto; staging as confirm-or-kill on the **customer** path.

**Rejected**

- Five-pass pass. `RESEARCH_READY_FOR_PROGRAMMING`. Waiving Q4/Q8/Q12/Q19/Q20. Merging 007+009 into one DHAN clock. Treating 5m YAML signal as 2RnBT9 3m. 05 overlaying three primaries. Invented win rates / lots / fills.

**UNKNOWN / DATA_INSUFFICIENT**

- 3m/2m resample; ST ATR seed vs Dhan charts; Super Scalper lengths; OF history; F&O 15:40 circular; SENSEX step; HQ option history; look-ahead in resample; liquidity gate.

Next: 00 must not paper over §4. 06 fixtures only — **not** a pass. Real five-pass only after §5.
