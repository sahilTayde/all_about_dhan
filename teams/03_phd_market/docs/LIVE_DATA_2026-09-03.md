# LIVE DATA — 2026-09-03 (paper-probe)

**Team:** 03_phd_market  
**Layer:** `VALIDATION` (this file). Not a strategy. Not a fill tape.  
**Status:** `DRAFT` / `UNVALIDATED`  
**Gate:** **not** `RESEARCH_READY_FOR_PROGRAMMING`  
**As-of:** 2026-09-03 ~13:26 IST (`python -m dhan_client --live paper-probe` succeeded; Founder Data API **Active**)

No secrets in this note. **No client IDs, tokens, or `.env` values.** No win rates. No invented quotes, lots, or bars beyond the probe fields listed here. `place_order` remains **SafeMode refused**.

Rate-limit table (official intro + option-chain child page): [`packages/dhan-client/docs/RATE_LIMITS.md`](../../../packages/dhan-client/docs/RATE_LIMITS.md).

Yaml scrips: [`config/workspace.yaml`](../../../config/workspace.yaml) `markets[]`.

---

```text
HANDOFF
From:     teams/03_phd_market
To:       00 / 02 / 04 / 05 / 06 / 09
Date:     2026-09-03
Status:   DRAFT / UNVALIDATED — live paper-probe VALIDATION
Gate:     NOT RESEARCH_READY_FOR_PROGRAMMING

Accepted:
- Yaml IDX_I scrips 13 / 25 / 51 returned live LTP + chain today (CONFIRMED as
  working IDs this session — not eternal).
- SENSEX nearest expiry 2026-09-03 = session date → EXPIRY analog.
- Orders still refused. Chain 1 unique / 3s. HQ intraday enum has 5m, not 3m.

Rejected:
- Freezing lots from this chain snapshot.
- Treating IDX_I 5m volume arrays as proven VWAP tape.
- Inventing more quotes / fills / win rates.

UNKNOWN / DATA_INSUFFICIENT:
- Whether INDEX 5m `volume` is authentic trade volume (02: cash-index VWAP
  unsupported). Instrument-master lot/expiry weekday as eternal constants.
```

---

## 1. Yaml scrips 13 / 25 / 51 — CONFIRMED working **today**

`markets[]` maps NIFTY→**13**, BANKNIFTY→**25**, SENSEX→**51**, all `dhan_underlying_seg: IDX_I`. Yaml comments still say **VERIFY FROM instrument master**. This probe does **not** make those integers eternal.

| Yaml id | Scrip | Seg (yaml) | Option quote seg (yaml) | Live this probe |
|---------|-------|------------|-------------------------|-----------------|
| NIFTY | 13 | IDX_I | NSE_FNO | LTP request included `"13"`; expirylist + chain **200** |
| BANKNIFTY | 25 | IDX_I | NSE_FNO | LTP included `"25"`; expirylist + chain **200** |
| SENSEX | 51 | IDX_I | BSE_FNO | LTP included `"51"`; expirylist + chain **200** |

**Also observed (do not invent more):**

- `GET /profile` **200**; `dataPlan` **Active**.
- `POST /marketfeed/ltp` IDX_I `[13, 25, 51]` → **success**.
- Orders: `place_order` **SafeMode refused** (unused Order API 10/s budget).

**03 verdict:** these three scrips are **CONFIRMED as working Dhan IDX_I IDs on 2026-09-03**. Re-read the instrument master before treating them as forever. SENSEX chain still quotes on **BSE_FNO** per yaml — this probe did not contradict that; it also did not dump a full token table.

---

## 2. Option chain — nearest expiry / last_price / strike_count

Probe truncated bodies to `last_price` + `strike_count`. **No lot size frozen.** `expiry: FROM_CONTRACT`.

| Underlying | Scrip | Nearest expiry (list) | Chain last_price | strike_count |
|------------|-------|------------------------|------------------|--------------|
| NIFTY | 13 | **2026-09-08** | ~**23904** | **226** |
| BANKNIFTY | 25 | **2026-09-29** | ~**57495** | **357** |
| SENSEX | 51 | **2026-09-03** | ~**76590** | **170** |

NIFTY expiry list head (probe): 2026-09-08, 09-15, 09-22, 09-29, 10-06, 10-27 — weekly-looking **this vintage**, still `FROM_CONTRACT`.  
BANKNIFTY list head: 2026-09-29, 10-27, 11-23, 12-29, … — **monthly-looking** this vintage (weekly discontinued under one-weekly-per-exchange remains **VERIFY**, not a freeze).  
SENSEX list head: 2026-09-03, 09-10, 09-17, 09-24, … — weekly-looking **this vintage**; **nearest = session date**.

### SENSEX → `EXPIRY` analog

Session calendar date **2026-09-03** equals SENSEX nearest sheet **2026-09-03**.

**03 verdict:** tag this session **`EXPIRY` for SENSEX** (CAS-004 analog / nightly `SessionKind`). NIFTY nearest is **2026-09-08** (not today). BANKNIFTY nearest is **2026-09-29** (not today). Do **not** retune weekday folklore from this one print. Do not invent settlement math.

---

## 3. INDEX 5m charts — volume flag

`POST /charts/intraday` **5m**, instrument **INDEX**, `IDX_I`, each of 13/25/51:

| Field | Observed |
|-------|----------|
| `bar_count` | **276** each |
| `has_volume` | **true** |
| `has_oi` | **false** |

02 VALIDATION ([`VALIDATION_MATH.md`](../../02_phd_math/docs/VALIDATION_MATH.md)): session VWAP needs **traded** volume; cash **NIFTY 50 / SENSEX calculation is not a traded tape** — VWAP on cash index = **unsupported / misuse**. Spoken 2RnBT9: analyse **futures**.

**03 verdict:** HQ **returned a `volume` array** on INDEX 5m. That is a **plumbing fact**, not a microstructure proof.

**UNKNOWN:** whether those arrays are authentic exchange prints, a vendor composite, or a placeholder. **Do not** run STRAT-003 session VWAP/VWMA on IDX_I volume until 02/03 sign a tape object. Prefer **FUTIDX** (or the option) for VWAP. `has_oi: false` on INDEX is expected (OI lives on F&O, not the cash index calculation).

---

## 4. Spoken 3m vs HQ 5m

| Claim | Layer |
|-------|--------|
| STRAT-003 spoken **3-minute** futures | `SOURCE_FACT` (English bind CONFIRMED) |
| HQ `/charts/intraday` intervals | `{1, 5, 15, 25, 60}` only — **3m not a token** ([`RATE_LIMITS.md`](../../../packages/dhan-client/docs/RATE_LIMITS.md)) |
| This probe | **5m INDEX** path **200**, 276 bars |

**03 verdict:** keep `resample_hypothesis: 3m`. HQ-native confirm path remains **5m**. This live call does **not** create a 3m REST enum. Staging 5m ST/MACD confirm-or-kill stays **PROJECT_MIX** vs Gokul’s 3m entry.

---

## 5. Rate limits (cited, unused Order bucket)

From official intro + option-chain page (probe `rate_limits_cited` + RATE_LIMITS.md):

| Bucket | Cap | This workspace |
|--------|-----|----------------|
| Order | **10/s** | **Unused** — SafeMode |
| Data | **5/s** (day cap documented) | Charts gated |
| Quote | **1/s** | LTP gated |
| Non-trading | **20/s** | Profile |
| Option chain | **1 unique / 3s** | Expirylist **and** chain share the budget |

Desk full-chain poll stays **3m**. Do not default 1m full-chain.

---

## 6. What this does **not** confirm

- Lot sizes (none in truncated chain).  
- Bid/ask fills (no orders).  
- CAS IEP / indicative index (not in this probe).  
- FUTIDX OHLC (this path was **INDEX** `IDX_I`, not futures tokens).  
- Eternal expiry weekdays.  
- Win rates or STRAT expectancy.

**Orders remain refused.** Empty prior `DHAN_*` no longer blocks **data** probes when the Founder Data API is Active; it still does **not** license live trading.

---

## UNKNOWN / blockers

- INDEX 5m `volume` meaning (see §3).  
- Official lot circular PDF; F&O 15:40 circular still not in-repo.  
- Historical option quotes depth beyond this snapshot.  
- SENSEX strike step / tick still not extracted (170 strikes today is a **count**, not a step).

**No profitability claimed.**
