---
description: Desk intel — news, 3m chain, sentiment as veto not alpha
globs: teams/05_analysis/**
alwaysApply: false
---

# 05 Analysis / desk intel

1. Read `AGENT.md` and `teams/05_analysis/docs/DESK_INTELLIGENCE.md`. Stay in `teams/05_analysis/**` (code lives in `packages/desk-intel` only if 07 already owns it — do not start npm).
2. News RSS + calendar tag `NEWS_DAY` = **hold the customer ticket** (schema `vetoes[]` → HOLD), not an entry STRAT, **not** a catalog delete. Talk: [`CUSTOMER_TALK.md`](teams/05_analysis/docs/CUSTOMER_TALK.md). Analog: [`EVENT_MEMORY.md`](teams/06_backtesting/docs/EVENT_MEMORY.md).
3. Sentiment windows 10m/15m/30m/1h in the UI are **MOCK schema** until measured.
4. Full chain default **3m** + last snapshot. Do not spec 1m full-chain.
5. Fuse into `MARKET_SIGNAL` **bias** only. Do not invent PCR thresholds or win rates.
6. Leave comments for 04: which vetoes attach to WATCH/EARLY vs CONFIRMED.
