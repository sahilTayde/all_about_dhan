# LIVE OHLC — 2026-09-03 (INDEX 5m, not STRAT-003 tape)

**Team:** 02_phd_math  
**Layer:** `VALIDATION`  
**Status:** `DRAFT` / `UNVALIDATED`  
**Gate:** **not** `RESEARCH_READY_FOR_PROGRAMMING`  
**No win rates. No fills. No invented lots.**

06 ran all-three VWAP+VWMA+ST on live `POST /charts/intraday` **5m**, instrument **INDEX**, `IDX_I` scrips **13 / 25 / 51**. Spoken STRAT-003 is **FUTIDX 3m** ([`TRANSCRIPT_STRATEGY_BIND.md`](../../01_research/docs/handoffs/TRANSCRIPT_STRATEGY_BIND.md)). 03 live note: [`LIVE_DATA_2026-09-03.md`](../../03_phd_market/docs/LIVE_DATA_2026-09-03.md). Recon: `data/recon/BACKTEST_INDEX_5M_2026-09-03.json` (`metrics_claimed: false`, `win_rate: null`).

| Fact | 02 verdict |
|------|------------|
| INDEX `has_volume: true` on 5m IDX_I bars | **UNKNOWN** as a VWAP tape. Cash index is a **calculation**, not a traded lot. Do **not** treat this volume as **FUTIDX**. Session VWAP / VWMA on that array is **not** STRAT-003. Prefer futures (or option) volume when that path exists. Identity vs vendor composite = `UNKNOWN`. |
| HQ **5m** native vs spoken **3m** | Spoken 3m is bind **CONFIRMED**. `{1,5,15,25,60}` still has **no 3m**. This run is a **5m INDEX proxy**, not a 3m FUTIDX recipe. Grid: 5m native vs 1m→3m resample on **futures** — do not pick a winner from this file. |
| NIFTY counts: **425** bars; all-three CE **108**, PE **137**, SKIP **130** | **Compute counts**, not win rates, not expectancy, not option P/L. `win_rate` / `option_pnl` in recon are **null**. Same for BANKNIFTY / SENSEX rows in that JSON. |
| Supertrend 10,3 on this run | Still **inference** from 003 **“103” WEAK**. Not a frozen spoken param. |

**Rejected:** calling this a STRAT-003 backtest; cash-index VWAP as `supported`; publishing 108/137 as a hit rate.

Still **`UNVALIDATED`**. 06 EVENT_MEMORY: score **`NORMAL`** only — these counts are not a score sample.
